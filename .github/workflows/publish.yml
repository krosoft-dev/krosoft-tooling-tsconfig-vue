name: Publish

on:
  push:
    branches: [main]

jobs:
  publish:
    permissions:
      id-token: write
    uses: krosoft-dev/Krosoft.DevOps.GitHub/.github/workflows/npm-publish.yml@main
    with:
      run-build: false
      run-tests: false
